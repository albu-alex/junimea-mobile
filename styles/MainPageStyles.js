import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1
    },
    inner: {
        paddingTop: '15%',
        flex: 1,
        alignContent: 'center',
        backgroundColor: 'ghostwhite',
    },
});

export {styles}
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#DADADA'
    },
    inner: {
        paddingTop: '15%',
        flex: 1,
        backgroundColor: "#DADADA"
    },
});

export {styles}